#!/bin/sh
set -e

# Remove contents of /var/run/usbmount before the package is removed.
if [ "$1" = remove ]; then
    rm -f /var/run/usbmount/*
    if [ -f /etc/usbmount/usbmount.conf.back ]; then
	mv /etc/usbmount/usbmount.conf.back /etc/usbmount/usbmount.conf
    fi
fi



exit 0
